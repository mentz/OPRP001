#ifndef __UTILS_H__
#define __UTILS_H__
#include <stdlib.h>

/*
void * dMergeSort(void * vec, size_t elem_size, size_t arr_size);
void partition(void * vec, int start, int end);
void merge(void * vec, int start, int end);
*/

void swap(double * a, double * b);
void bubble_sort(double * vec, size_t arr_size);

#endif
